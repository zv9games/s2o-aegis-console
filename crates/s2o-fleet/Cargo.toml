[package]
name = "s2o-fleet"
version = "0.1.0"
edition = "2021"
authors = ["Split2ops Software"]
description = "S2O Aegis local fleet host inventory (file-backed)"

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
