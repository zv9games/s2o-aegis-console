[package]
name = "s2o-store"
version = "0.1.0"
edition = "2021"
authors = ["Split2ops Software"]
description = "S2O Aegis local event store (JSONL append-only MVP)"

[dependencies]
s2o-schema = { path = "../s2o-schema" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
